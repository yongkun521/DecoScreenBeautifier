# Bundled Windows Terminal (x64) Placeholder

将 Windows Terminal x64 Unpackaged ZIP 解压后的内容放到本目录。

最低要求：
- `WindowsTerminal.exe`

建议同时保留：
- `.portable`（可由启动器或构建脚本自动创建）
- 与当前版本匹配的上游资源文件

版本元信息请更新到：
- `../wt_version.json`
